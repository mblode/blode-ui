---
title: Date Range Dropdown
date: 2025-05-07
description: A dropdown for selecting a date range.
author: fingertip
published: true
---

<ComponentPreview name="date-range-dropdown-demo" />

## Installation

<Tabs defaultValue="cli">
  <TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  <TabsContent value="cli">

```bash
npx shadcn@latest add "https://ui.fingertip.com/r/styles/default/nge-dropdown"
```

  </TabsContent>
  <TabsContent value="manual">
    <Steps>
      <Step>Copy and paste the following code into your project.</Step>
      <ComponentSource name="date-range-dropdown" />
      <Step>Update the import paths to match your project setup.</Step>
    </Steps>
  </TabsContent>
</Tabs>

## Usage

```tsx
import { DateRangeDropdown } from "@/components/ui/date-range-dropdown";
```

```tsx
<DateRangeDropdown
  dateRangeOption={dateRangeOption}
  setDateRangeOption={setDateRangeOption}
  options={presets}
  fromDate={new Date(2023, 0, 1)} // Optional minimum date
/>
```

## Props

| Prop                 | Type                                | Default | Description                                  |
| -------------------- | ----------------------------------- | ------- | -------------------------------------------- |
| `dateRangeOption`    | `DateRangeOption`                   | `-`     | The currently selected date range option.    |
| `setDateRangeOption` | `(option: DateRangeOption) => void` | `-`     | Function to update the selected date range.  |
| `options`            | `DateRangeOption[]`                 | `-`     | Available date range options to choose from. |
| `fromDate`           | `Date`                              | `-`     | Optional minimum selectable date.            |
| `className`          | `string`                            | `-`     | The class name to apply to the component.    |

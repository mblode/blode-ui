---
title: Installation
description: Register the @blode namespace, add the design system, and install your first component.
---

Blode UI installs through the [shadcn CLI](https://ui.shadcn.com/docs/cli). Point `init` at the design system and it registers the `@blode` namespace, sets `blode-icons-react` as the icon library, and writes Blode's tokens into your CSS in one step.

<Steps>

### Set up the project

```bash
npx shadcn@latest init https://blode.co/ui/r/ui.json
```

### Add a component

```bash
npx shadcn@latest add @blode/button
```

### Import it

```tsx {1,6} showLineNumbers
import { Button } from "@/components/ui/button";

export default function Home() {
  return <Button>Click me</Button>;
}
```

</Steps>

## Already running shadcn

Register the namespace and add the design system yourself, so `init` leaves your existing `components.json` alone:

```bash
npx shadcn@latest registry add @blode=https://blode.co/ui/r/{name}.json
npx shadcn@latest add @blode/ui
```

Type `{name}` literally. It is the registry's URL template, and the CLI substitutes the component name on each request.

`@blode/ui` is the design system, and components depend on it. Their variants compile to `rounded-[var(--field-radius)]` and `h-[var(--field-height)]`; without the tokens those custom properties resolve to nothing and everything renders unstyled.

## Framework guides

Step-by-step setup for a specific stack:

- [Next.js](/docs/installation/next)
- [Vite](/docs/installation/vite)
- [Remix](/docs/installation/remix)
- [Astro](/docs/installation/astro)
- [Laravel](/docs/installation/laravel)
- [Gatsby](/docs/installation/gatsby)
- [Manual](/docs/installation/manual)
